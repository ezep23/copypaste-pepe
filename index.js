
function buscarRespuesta(){
    const busqueda = document.querySelector("#entrada")
    const componente = document.querySelectorAll("acordion-component")
    console.log(componente.entries(title))
    console.log(componente.entries(title))
    if (busqueda === componente.entries(title)){}
    e.preventDefault();
}